<%= form_with(model: favorite, local: true) do |form| %>
  <% if favorite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favorite.errors.count, "error") %> prohibited this favorite from being saved:</h2>

      <ul>
      <% favorite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tweet_id %>
    <%= form.text_field :tweet_id, id: :favorite_tweet_id %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text, id: :favorite_text %>
  </div>

  <div class="field">
    <%= form.label :user_name %>
    <%= form.text_field :user_name, id: :favorite_user_name %>
  </div>

  <div class="field">
    <%= form.label :created_at %>
    <%= form.date_select :created_at, id: :favorite_created_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
